import type { FC } from 'react'
import type { Repo } from '../../lib/types/Repo'

interface Props {
    repos: Repo[]
}

const RepoTable: FC<Props> = () => {
    return (
        <div>
            
        </div>
    )
}

export default RepoTable